{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Recreate worker (whisper ROCm)",
			"type": "shell",
			"command": "docker compose up -d --force-recreate worker",
			"problemMatcher": [],
			"group": "build"
		}
	]
}
